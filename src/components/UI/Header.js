import styles from './Header.module.css'

const Header = () => {
    return (
        <h2 className={styles.head}>Enter data to create your own Profile Card!</h2>
    )
}

export default Header